---
// Astro emits this route as /404.html in static builds. Render serves that
// file for unmatched paths and preserves the original 404 response status.
export const prerender = true;

import InnerPageLayout from '@/layouts/InnerPageLayout.astro';
import Ledger from '@/components/Ledger.astro';
import LedgerRow from '@/components/LedgerRow.astro';
import Button from '@/components/Button.astro';
// CONSENT.md:32
import trailPhoto from '@/assets/images/photos/oo-corridor.jpg';

const destinations = [
  {
    href: '/about',
    label: 'About',
    detail: 'Meet the community and learn how our seasons work.',
  },
  {
    href: '/community',
    label: 'Community',
    detail: 'What members do beyond practice.',
  },
  {
    href: 'https://tcsc.ski/',
    label: 'Registration',
    detail: 'Practice sign-ups and club registration live at tcsc.ski.',
  },
];
---

<InnerPageLayout
  title="Page not found · Twin Cities Ski Club"
  description="Page not found."
  robots="noindex"
  headline="This trail ends here."
  subhead="The page may have moved, or the link may point to our old site."
  photo={trailPhoto}
  photoAlt="A freshly groomed ski trail running through a forest coated in snow"
  photoPosition="center 60%"
>
  <section class="gutter mx-auto max-w-site band-y">
    <div class="grid gap-14 md:grid-cols-12 md:gap-10">
      <div class="md:col-span-6 lg:col-span-5">
        <p class="label-caps text-mint-deep">404 · Page not found</p>
        <Button href="/" variant="on-paper" class="mt-6">Back to home</Button>
      </div>

      <nav aria-label="Try one of these" class="md:col-span-5 md:col-start-8">
        <h3 class="type-h3 text-navy">Try one of these</h3>
        <Ledger cols="stacked" rule="top" surface="paper" class="mt-4">
          {destinations.map((destination) => (
            <LedgerRow
              title={destination.label}
              note={destination.detail}
              href={destination.href}
            />
          ))}
        </Ledger>
      </nav>
    </div>
  </section>
</InnerPageLayout>
