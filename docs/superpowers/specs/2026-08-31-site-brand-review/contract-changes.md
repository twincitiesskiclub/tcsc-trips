# contract changes: DESIGN.md v1 to v2

every line is one change in `DESIGN.md` v2's `## changelog from v1`. striking a line rejects that change: the clause reverts to v1 and the ledger rows it unlocks move to `## rejected` with reason "contract change struck at gate 1".
check a line to approve it. the fixed identity items (nine tokens, two families, real consented photos, the Live Conditions device) are not on this list because they did not change.

- [ ] theme: add "the quiet ledger wins" as the tiebreaker sentence: reviewers kept re-litigating the june and july posture (unlocks L-014, L-052, L-069)
- [ ] color, tokens: replace the hex reference column with resolved values and record the flask navy difference as accepted: v1's estimates shipped into `theme-color` and the logo fills (unlocks L-019, L-020, L-127)
- [ ] color, token uses: mint is the hero h1, the CTAStrip h2, live data, labels, links, and CTA fills; paper-card is a placeholder tint, not a box fill: the site's calmer reading of mint (unlocks L-052, L-040)
- [ ] color, register: "single moment of paper" becomes two named paper moments on home (mission panel, closing wax-room-plus-sponsors chapter), never adjacent (unlocks L-050)
- [ ] color, register rules: slate not opacity on paper; three tones on navy; headings on navy are paper except the hero h1 and CTAStrip h2; coral is a state; the compact strip gets the live dot; accent budget per surface (unlocks L-001, L-081, L-052, L-048, L-051, L-128)
- [ ] color, links: three link roles as classes (`.link-inline`, `.link-ledger`, `.link-nav`), no other recipe (unlocks L-045)
- [ ] color, hairlines: two weights per surface as `.hairline` / `.hairline-soft`; chrome edges move from mint/10 to mint/15 (unlocks L-021)
- [ ] color, buttons: one Button primitive, one recipe per surface, no paper hover on navy, content-width everywhere (unlocks L-043)
- [ ] color, data colors: the four wax chip colors become tokens with a chip-only rule (unlocks L-078)
- [ ] color, literals: navy and ink through `theme()`, logo fills the one literal exception at the resolved hex (unlocks L-019, L-127)
- [ ] color, contrast: replace the table with measured values and add the measure-before-ship rule and the AA floor (unlocks L-020, L-001)
- [ ] color, banned: the gradient exception names the two functional scrims; a third dark is banned; arbitrary color values are banned (unlocks L-120, L-040)
- [ ] typography, families: describe what ships (Archivo Variable, PolySans BulkyWide single 700 cut, punctuation from Archivo) instead of Söhne and PolySans Median (unlocks L-012, L-082, L-016)
- [ ] typography, display cut: "2 to 3 places per page" becomes "h1, h2, and the prominent temperatures only; never h3, paragraphs, stat values, rows, buttons, labels" (unlocks L-011, L-016, L-042, L-122, L-123, L-046)
- [ ] typography, scale: every role is a named clamp() utility defined once; new rows for long-form h2, statement, long-form body, stat, label-caps, seam, button; display weight 700; inner h1 desktop 4.0rem (unlocks L-010, L-012, L-013, L-072, L-073, L-075, L-077, L-018)
- [ ] typography, mechanics: `--body-lh` by surface; two measures plus a 44ch statement cap; balance on h1 and h2; captions never uppercase; button label sizes; prose headings themed once; FactStack stat spec (unlocks L-015, L-074, L-076, L-121, L-043, L-017, L-042)
- [ ] typography, labels and seams: the eyebrow clause becomes two utilities (`.seam`, `.label-caps`), seams once per band and at most three per page, data labels do not count, uppercase banned on headings, buttons, dates, times, floor 0.75rem (unlocks L-014, L-126, L-135, L-041)
- [ ] typography, outline: every band has a heading element; the seam becomes the h2 when no heading is passed (unlocks L-125)
- [ ] layout, gutter: one clamp token and one `.gutter` utility, no `px-*` beside it (unlocks L-023)
- [ ] layout, container: one 1280 container everywhere and one reading column on the grid, centered at columns 3 to 10 (left-aligned was reverted after the preview walk); the 1080 inner width is dropped (unlocks L-024, L-054)
- [ ] layout, rhythm: three named band rhythms, the same-surface adjacency rule, one seam gap, mission panel padded both sides, prose first-h2 rule (unlocks L-022, L-053, L-142)
- [ ] layout, photo groups: one grammar (flush, hairline gaps, no border, no empty cell, portrait never wide, mobile rhythm) (unlocks L-059, L-060, L-062, L-063, L-064, L-106, L-136, L-137)
- [ ] components, primitives: add Seam, Ledger, FactStack, Button, PhotoStrip, SectionHeader, ProseColumn, WaxEntryRow with fixed values (unlocks L-040, L-041, L-042, L-043, L-044, L-046, L-106, L-107)
- [ ] components, table rewritten to the shipped site: HeroInner, MissionPanel, SeasonsGrid, PhotoMosaic, Lightbox, CoachEntry, TripsTable, SponsorWall, WaxRoomFeed, WaxEntry, CTAStrip, Footer, MobileNavPanel, Nav (unlocks L-069, L-009, L-055, L-056, L-057, L-058, L-067, L-068, L-122, L-129, L-133, L-134, L-111, L-050)
- [ ] components, removed: TripEntry deleted; dead variants and props listed for deletion (unlocks L-108, L-140)
- [ ] signature device: v1 body kept verbatim; "as built" addendum records the real venues, the Trail report name, the clock-time stamp, the fever cell and its ♪, no expand-on-click, compact placement under the nav, the dryland dateline, the unavailable single line, cell rules on the right edge (unlocks L-091, L-141, L-065, L-123, L-124, L-138, L-119, L-051)
- [ ] motion: rewritten as an explicit arrivals list with one easing token; hero timing as shipped plus the CTA joining the gesture; conditions first fill replaces the refresh flicker; mobile panel codified; lightbox 200ms entrance kept as CSS-only; hover is color only; focus rings never transition; delays zeroed under reduced motion; pointer cursor on buttons (unlocks L-047, L-113, L-114, L-117, L-118, L-119, L-110, L-049)
- [ ] iconography: Lucide replaced by the three inline strokes that ship; arrow glyphs banned in controls; ♪ exception recorded (unlocks L-120, L-138)
- [ ] imagery: consent rule around CONSENT.md; watermark ban; crop rule with `position: attention`; masthead band geometry and per-page positions; webp everywhere with avif on the hero only; no BlurHash; orphan assets rule; srcset ceiling (unlocks L-007, L-102, L-062, L-063, L-059, L-130, L-131, L-132, L-116, L-101, L-115, L-129)
- [ ] og image: new section, one default card with the hero scrim and the logo lockup, no headline, og meta always set (unlocks L-070)
- [ ] 404: new section, one display heading, kicker, button, three-row ledger, middot title (unlocks L-034, L-126, L-085)
- [ ] empty states: new section, one ruled placeholder row in the ledger, honest sentence, one link, no invented schedule (unlocks L-061, L-035, L-036)
- [ ] copy: new section with the july 18 voice rules verbatim plus punctuation, names and facts, house formats, masthead-fact rule, one-name rule, titles and meta, alt text, single source (unlocks L-004, L-005, L-028, L-029, L-030, L-031, L-032, L-033, L-037, L-038, L-084, L-086, L-087, L-088, L-089, L-090, L-092, L-093, L-094, L-095, L-096, L-097, L-098, L-099, L-100, L-103, L-104, L-105)
- [ ] registration copy: new per-state, per-render-site table; labels never name a season; coming-soon CTA links out to tcsc.ski; state-aware card note; closed names the reopen months; one date-pair format; new-member date while only returning is open (unlocks L-003, L-006, L-025, L-026, L-027, L-071)
- [ ] banned: stat-box line rewritten around FactStack; the hero-arrow reservation dropped; perimeter borders, hover lifts, row washes, uppercase misuse, arbitrary utilities, opacity-for-tokens, hand-built patterns, dead CSS added (unlocks L-139, L-120, L-060, L-040, L-081, L-108, L-109, L-079)
- [ ] accessibility: AA floor; focus ring by nearest surface with an inset mint ring on tiles; 44px on inline links and the venue link; pointer cursor; outline rule (unlocks L-002, L-049, L-117, L-125)
- [ ] build guards: new section, a dist-grep test for the primitives (unlocks L-112)
